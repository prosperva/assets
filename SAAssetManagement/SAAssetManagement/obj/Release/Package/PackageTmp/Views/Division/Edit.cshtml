@model SAAssetManagement.Models.Division

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Division</legend>

        @Html.HiddenFor(model => model.DivisionID)

        <div class="editor-label">
            @Html.LabelFor(model => model.DivisionName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DivisionName)
            @Html.ValidationMessageFor(model => model.DivisionName)
        </div>
        @*<div class="editor-label">
            @Html.LabelFor(model => model.EmployeeID,"Division Contact")
        </div>
        <div class="editor-field">
            @Html.DropDownList("employeeid",String.Empty)
            @Html.ValidationMessageFor(model => model.EmployeeID)
        </div>*@

        <input type="hidden" name="CreatedOn" value="@Model.CreatedOn" />

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
